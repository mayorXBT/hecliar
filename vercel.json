{
  "framework": "nextjs",
  "installCommand": "npm install --include-workspace-root --workspace frontend --workspace @hecliar/game-logic",
  "buildCommand": "npm run build --workspace frontend",
  "outputDirectory": "frontend/.next"
}
